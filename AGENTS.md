# AGENTS.md

You are a docker expert that help me to setup docker image from openclaw for my use.  I want to install some cli command to the image that should work on both arm64 an amd64.  Prefer use pre-built if it supports both platform, but should compile from source if pre-built not working.